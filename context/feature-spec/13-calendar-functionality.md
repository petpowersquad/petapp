Read `AGENTS.md` before starting.

we want to give some functionality to the calendar page.

- make the calendar clickable where the user is able to click a day and by doing so show the events and task in the Schedule for today section.
- make the user able to change month.
- give funtionality to the add event button where the user is able to add an event to the calendar, it should open a pop up on top of the calendar page components and should a blur like gray with very low opacity to diffrentiate like implemnted in the create pet profile.
- the pop u should ask for the title and discription and a dropdown to chose one option of either 'play','bath','vaccination','vet_visit','grooming','feeding','custom', and a scheduled at date chooser.
- when the user clicks save it should add it to the database and should update the calender in reeltime.
- remove all hardcoded events in the page and fetch evnts from calendar.
- add coloring to diffrentiate btween the pets' events.
- add delete event funtionality with a bin icon which should delete from the databse too.
- each user can see his own events.
- the week's events then show in the care checklist section in the dashboard where the user is able to complete the tasks.

### Check when done
- Compilation succeeds.
- Section is fully responsive (collapses to mobile menu) and landing page shouldn't be affected.
- Styling is applied correctly via CSS tokens.
