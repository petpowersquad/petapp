Read `AGENTS.md` before starting.

We want to implement a dropdown in the scan page where the user has to chose which pet he is scanning.

- edit `app/scan/page.tsx`
- use shadcn dropdown in the scan section header with all the pets that the user own as options.
- use website them and colors and refer to `ui-context.md`.
- do not implement scan functionality keep as it is for now just do the dropdown.

### Check when done
- Compilation succeeds.
- Section is fully responsive (collapses to mobile menu) and landing page shouldn't be affected.
- Styling is applied correctly via CSS tokens.