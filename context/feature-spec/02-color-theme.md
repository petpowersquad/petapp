Read `AGENTS.md` before starting.

We're trying to make all the components of this webapp all follow the same theme.

Edit `globals.css` and ensure all components match the existing theme in `ui-context.md`.

### Check when done
- All components import without errors
- All components follow the same theme in `ui-context.md`